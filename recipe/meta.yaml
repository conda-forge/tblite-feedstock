{% set name = "tblite" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/tblite/tblite/releases/download/v{{ version }}/tblite-{{ version }}-source.tar.xz
  sha256: 0b0bf8eb907766eff9c53dabf1d4dd9f6469e9c847d616fcc0c005114a491252

build:
  number: 0
  skip: true  # [win]
  script:
    - set -ex
    - meson setup _build ${MESON_ARGS:---prefix=${PREFIX} --libdir=lib} --buildtype=release
    - meson compile -C _build
    - meson test -C _build --no-rebuild --print-errorlogs --suite unit  # [build_platform == target_platform]
    - meson install -C _build --no-rebuild

  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('fortran') }}
    - meson
    - ninja
    - asciidoctor
    - pkg-config
    - llvm-openmp  # [osx]
    - libgomp  # [linux]
  host:
    - libblas
    - liblapack
    - mctc-lib
    - dftd4
    - simple-dftd3
    - toml-f

test:
  requires:
    - pkg-config
  commands:
    - {{ name }} --version  # [build_platform == target_platform]
    - pkg-config {{ name }} --modversion
    - test -f $PREFIX/include/{{ name }}.h
    - test -f $PREFIX/lib/lib{{ name }}$SHLIB_EXT

about:
  home: https://github.com/grimme-lab/tblite
  license: LGPL-3.0-or-later
  license_file:
    - COPYING
    - COPYING.LESSER
  summary: Light-weight tight-binding framework
  doc_url: https://tblite.readthedocs.io

extra:
  recipe-maintainers:
    - awvwgk
